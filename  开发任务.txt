目前：
搭建主菜单{
->开始游戏时就要给玩家一个强化机会
}
暂停菜单
构建关卡系统:一个关卡等于一个boss
{
->从levels.json读取数据，创建并绑定boss
}
构建货币系统
完成存档功能
想法：
换个字体，并且让raylib能显示中文{
->利用码点加载中文字符{
参考：https://www.bilibili.com/opus/935069549245497384
本质：LoadFontMemory()默认只加载部分字符，只有提供码点表才会加载对应字符
只要在绘制之前加载了码点表即可
-->添加ResourceManager::fontFile，在内存保存总的字体文件
-->添加ResourceManager::textToDraw:vector<int*>保存当前帧需要绘制的所有文字的码点
-->修改ResourceManager::loadFont()函数，改成加载中文码点表
-->提供ResourceManager::unloadFont()卸载当前帧绘制的所有码点，必须要在EndDrawing()以后调用
}
->显示中文{
-->获得文本并添加到textToDraw
-->调用UI::drawText()
--->函数内loadCodePoints(),loadFont(),UnloadCodePoints(),然后绘制
}
}
调整UI{
	Button背景支持图片，
	背景绘制优化，
}
添加json的检查，缺失元素时明确提示而不是崩溃
写个工程文档(可以让ai代劳)

