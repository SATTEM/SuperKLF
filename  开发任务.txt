目前：
构建关卡系统:一个关卡等于boss or shop or event 
{
->完成Entity三类的复制构造函数
-->完成level::update()
-->LevelManager::initialize()加载关卡
->从levels.json读取数据，创建并绑定boss
->完成level advance时的关卡选择（终点都是boss level）
[修改主逻辑，让level通过StageController来管理各种循环，main只负责管理现在的关卡]
}
构建货币系统
完成存档和设置功能
想法：
调整UI{
	Button背景支持图片，
	背景绘制优化，
}
添加音效
添加json的检查，缺失元素时明确提示而不是崩溃
写个工程文档(可以让ai代劳)

