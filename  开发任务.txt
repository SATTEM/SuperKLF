目前：
完成存档和设置功能，各种默认值放到json
难度曲线：BattleLevel根据新增的passRange抽取

更长期的计划：
添加更多的Effect功能:在EntityModifier类添加静态方法

调整UI{
	Button背景支持图片，
	背景绘制优化，
	UI全面改造（buttonWithImage，drawBackground等）
}
添加音效
添加json的检查，缺失元素时明确提示而不是崩溃
写个工程文档(可以让ai代劳)

